{
  "name": "brand-color-accessibility-tool",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "test:e2e:ci": "playwright test --project=chromium",
    "test:e2e:ui": "playwright test --ui",
    "capture-milestone": "tsx scripts/capture-visual-milestone.ts",
    "lighthouse": "tsx scripts/capture-lighthouse.ts"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.0",
    "@playwright/test": "^1.49.1",
    "@vitest/coverage-v8": "^4.0.15",
    "lighthouse": "^12.8.2",
    "tsx": "^4.21.0",
    "typescript": "~5.6.2",
    "vite": "^7.2.7",
    "vitest": "^4.0.15"
  },
  "dependencies": {
    "lit": "^3.2.1"
  }
}
